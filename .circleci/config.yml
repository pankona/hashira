version: 2
jobs:
  build:
    working_directory: /go/src/github.com/pankona/hashira
    docker:
      - image: pankona/godev
    steps:
      - checkout
      - run:
          name: dep ensure
          command: dep ensure
      - run:
          name: test
          command: make test
      - run:
          name: build
          command: make

workflows:
  version: 2
  build:
    jobs:
      - build
